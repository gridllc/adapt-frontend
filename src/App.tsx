import React from 'react';

/**
 * @deprecated This component is no longer in use and is scheduled for deletion.
 * The application's main entry points are now managed by the router in `index.tsx`,
 * with `HomePage.tsx` and `TrainingPage.tsx` serving as the primary page components.
 * All relevant logic from this file has been migrated to `src/pages/TrainingPage.tsx`.
 */
const App: React.FC = () => {
  return null;
};

export default App;
